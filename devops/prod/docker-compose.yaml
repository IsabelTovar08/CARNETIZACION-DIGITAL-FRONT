version: "3.8"

services:
  carnetizacion-digital-front-prod:
    container_name: carnetizacion-digital-front-prod
    build:
      context: ../../
      dockerfile: Dockerfile
      args:
        NODE_ENV: prod
        API_BASE_URL: ${API_BASE_URL}
    ports:
      - "4600:80"
    environment:
      NODE_ENV: prod
      API_BASE_URL: ${API_BASE_URL}
    restart: always
    networks:
      - network_prod

networks:
  network_prod:
